// Set to true only for local development when you want to bypass login and use a fixed token.
export const USE_MANUAL_TOKEN = false;

// Paste your JWT token here when testing locally.
export const MANUAL_TOKEN = "";