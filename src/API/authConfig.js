// Keep this false for production/live usage so admin login is verified by the backend database.
export const USE_LOCAL_TOKEN = false;

// Only use this for local testing if your backend allows a fixed token flow.
export const LOCAL_JWT = "";