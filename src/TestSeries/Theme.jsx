
import "@fontsource/poppins";

import { createTheme } from "antd/styles";
const theme = createTheme({
  typography: {
    fontFamily: "Poppins, Helvetica, sans-serif",
    fontWeightRegular: 400
  },
});
export default theme;